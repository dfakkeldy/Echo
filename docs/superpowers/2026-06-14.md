I can't run commands in this particular session (it's read-only), so I'll give you exact commands — and I can run the whole investigation myself if you continue in the main chat. But first, the important reasoning, because the symptoms point at two different things, and only one of them is alarming.

First: my reconciliation cannot explain this
The tool I ran only ever moves files (every move is in reconcile_undo.csv) and it only touched audiobook units. It never deletes, and it can't have removed an epub or emptied "lots" of folders beyond the ~33 it quarantined. So a widespread empty-folder problem is coming from somewhere else — which actually matters, because it means the data is likely still recoverable elsewhere.

Two of your observations are expected, not data loss
_reconcile_leftovers/ full of empty folders — that's by design. Those are the junk author folders (Apple, Magic Pill, Six Thinking Hats, …) whose audiobooks I moved to the correct author. The books are now under the right authors; the emptied shells got parked there for you to delete. Empty = working as intended.
_dupes_backup_from_icloud/ folders with no files — this is the iCloud backup that failed earlier (fileproviderd/iCloud couldn't serve the offloaded files, so rsync/ditto created the directory tree but copied no data). Critically: the iCloud originals were never deleted — that step was left pending. So those files should still be in iCloud (likely offloaded, needing "Download Now"), not gone.
The genuinely concerning part
"Emotional Design epub missing" and "lots of [author] folders with no files" — the reconciliation didn't touch ebooks, so this is a separate issue (most likely the original copy never pulled the ebooks down from iCloud because they were offloaded, or a sync gap). I can't confirm which without looking.

⚠️ Do this now to protect the recovery copy
Pause Syncthing on both the Mac and Bazzite before anything else, and don't empty any Trash. If the Bazzite mirror still has these files, a running sync could propagate the missing-file state and wipe the good copy. (Syncthing web UI → http://127.0.0.1:8384 → pause the books-library folder, on both machines.)

Diagnostics — read-only, change nothing
# Is the Mac library broadly intact, or broadly empty?
du -sh "/Volumes/Fledging-WD-2TB/Books"
find "/Volumes/Fledging-WD-2TB/Books" -type d -empty -not -path '*/_reconcile_leftovers/*' -not -path '*/_dupes*' | wc -l

# Find Emotional Design / Don Norman ANYWHERE — Mac drive, then iCloud source:
find "/Volumes/Fledging-WD-2TB" -iname '*emotional design*' 2>/dev/null
find ~/Library/Mobile\ Documents/com~apple~CloudDocs/Books -iname '*emotional design*' -o -iname '*norman*' 2>/dev/null

# THE KEY ONE — does the Bazzite mirror still have it? (recovery source)
ssh dan@100.95.69.48 'du -sh "/var/mnt/2TB Internal HDD/Books"; find "/var/mnt/2TB Internal HDD/Books" -iname "*emotional design*"'

# How many epubs survive in total, on each side:
find "/Volumes/Fledging-WD-2TB/Books" -iname '*.epub' | wc -l
ssh dan@100.95.69.48 'find "/var/mnt/2TB Internal HDD/Books" -iname "*.epub" | wc -l'
The result of the Bazzite du -sh + epub count vs the Mac one is the whole story: if Bazzite is much larger / has the epubs, that's your clean recovery source (and pausing sync just protected it). If both are missing them, then the ebooks likely never made it off iCloud in the first place — in which case the iCloud source (never deleted) is the master.

Want to paste those outputs here, or drop back into the main chat and I'll run all of it myself (including tracing Emotional Design through the undo log and checking the iCloud source)? My honest read: this is very likely recoverable — between the untouched iCloud originals, the Bazzite mirror, and my move-only undo log, I don't see a path where the data is actually gone — but I want to look rather than keep reasoning at yo