app_identifier("com.echo.audiobooks") # Your main bundle ID

# Apple ID is intentionally not stored in version control. Set FASTLANE_USER
# locally or in CI for lanes that need account authentication.

team_id("8AXNA5B37R") # Put your real Team ID here

# Since you have macOS and watchOS extensions, you can explicitly add them too:
for_lane :beta do
  app_identifier([
    "com.echo.audiobooks",                     # iOS app AND native macOS app (universal — same bundle id)
    "com.echo.audiobooks.watchkitapp",
    "com.echo.audiobooks.watchkitapp.widget"
  ])
end
